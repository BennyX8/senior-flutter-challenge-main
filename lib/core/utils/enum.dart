enum ErrorTypes {
  data,
  server,
  secket,
  handshake,
  socket,
  format,
  timeout,
  unknown
}

enum ApiStatus { success, failed }

enum RequestType { json, formData, map }
enum Environment{staging, production}
